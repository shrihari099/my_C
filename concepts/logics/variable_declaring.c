#include <stdio.h>

int main(){
    // Initializing variables of different data types
    int num = 1;         // integer variable with value 1
    float num2 = 9.1;    // float variable with value 9.1
    char ch = 'a';       // character variable with value 'a'
    double num3 = 8.777777;   // double variable with value 8.777777

    // Printing the values of the variables
    printf(" The integer is %d \n ", num);
    printf(" The float is %f \n ", num2);
    printf(" The double is %lf \n ", num3);
    printf(" The character is %c \n ", ch);
    
    return 0;
}

/* 
Overall, the program is well-written and easy to read. 
The variable names are descriptive and the code is properly indented. 
However, it is advisable to use more meaningful names for the variables, 
especially when writing more complex programs with many variables.
*/

